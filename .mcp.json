{
  "mcpServers": {
    "questlog": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"],
      "env": {
        "QUESTLOG_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
